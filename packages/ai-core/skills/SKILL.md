---
name: ai-core
description: "Use when: AI/ML core utilities, model inference, or AI pipeline orchestration in the memora codebase."
---

# AI Core Skill

Use `@memora/ai-core` for AI/ML core utilities and abstractions.

## Package Info

- Built with `tsdown` (ESM, with declarations).
- Entry: `src/index.ts`
- Scripts: `build`, `dev` (watch mode).

## Examples

```ts
import {} from "@memora/ai-core";
```
